% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.coxDRmodels.R
\name{predict.coxDRmodel}
\alias{predict.coxDRmodel}
\alias{predict.coxplsDRmodel}
\alias{predict.coxsplsDRmodel}
\alias{predict.coxDKsplsDRmodel}
\title{Predict method for DR-based Cox component models}
\usage{
\method{predict}{coxplsDRmodel}(
  object,
  newdata,
  comps = ncol(object$tt_plsDR),
  type = c("lp", "risk", "expected", "terms", "scores"),
  se.fit = FALSE,
  reference = c("strata", "sample", "zero"),
  y = NULL,
  weights = NULL,
  verbose = TRUE,
  ...
)

\method{predict}{coxsplsDRmodel}(
  object,
  newdata,
  comps = ncol(object$tt_splsDR),
  type = c("lp", "risk", "expected", "terms", "scores"),
  se.fit = FALSE,
  reference = c("strata", "sample", "zero"),
  y = NULL,
  weights = NULL,
  verbose = TRUE,
  ...
)

\method{predict}{coxDKsplsDRmodel}(
  object,
  newdata,
  comps = ncol(object$tt_DKsplsDR),
  type = c("lp", "risk", "expected", "terms", "scores"),
  se.fit = FALSE,
  reference = c("strata", "sample", "zero"),
  y = NULL,
  weights = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{An object returned by one of the DR-based fitting functions
with \code{allres = TRUE}.}

\item{newdata}{An optional data frame or matrix containing original
covariates. If omitted, predictions are computed on the training data.}

\item{comps}{Number of latent components to use for prediction.}

\item{type}{Type of predicted value. Choices are the linear predictor
(\code{"lp"}), the risk score exp(lp) (\code{"risk"}), the expected number
of events (\code{"expected"}), the terms of the linear predictor
(\code{"terms"}) or the latent component scores (\code{"scores"}).}

\item{se.fit}{If \code{TRUE}, pointwise standard errors are produced by the
underlying Cox model.}

\item{reference}{Reference level used to center relative predictions. This
is passed to \code{\link[survival]{predict.coxph}} and affects
\code{type = "lp"}, \code{"risk"} and \code{"terms"}.}

\item{y}{Optional \code{\link[survival]{Surv}} response to use with
\code{type = "expected"} when predicting on genuinely new observations.}

\item{weights}{Optional case weights used when rebuilding a model matrix for
formula-based fits.}

\item{verbose}{Should some details be displayed?}

\item{\dots}{Additional arguments passed to \code{\link[survival]{predict.coxph}}.}
}
\value{
A vector, matrix or list of predictions depending on \code{type} and
\code{se.fit}.
}
\description{
This function provides prediction methods for the rich objects returned by
\code{coxplsDR(..., allres = TRUE)}, \code{coxsplsDR(..., allres = TRUE)}
and \code{coxDKsplsDR(..., allres = TRUE)}.
}
\seealso{
\code{\link[survival]{predict.coxph}}, \code{\link{coxplsDR}},
\code{\link{coxsplsDR}}, \code{\link{coxDKsplsDR}}
}
